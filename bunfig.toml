# Every test file loads this first, so no test can write into lookout's own
# checkout. See test/setup.ts for why that needs to be a suite-wide guarantee
# rather than something each test remembers to do.
[test]
preload = ["./test/setup.ts"]
